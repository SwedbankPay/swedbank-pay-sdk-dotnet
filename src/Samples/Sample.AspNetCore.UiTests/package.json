{
  "name": "sample.aspnetcore.uitests",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "cypress": "^16.0.0",
    "typescript": "^7.0.2"
  },
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run -b chrome",
    "cy:run:dev": "cypress run -b chrome --env baseUrl=https://swedbankpay-sdk-001-dev.azurewebsites.net/",
    "cy:run:uat": "cypress run -b chrome --env baseUrl=https://swedbankpay-sdk-001-uat.azurewebsites.net/"
  }
}
